import Image from "next/image"
import type { WhiteProduct } from "@/lib/wow-white-products-data"

export function WowWhiteProductDetail({ product }: { product: WhiteProduct }) {
  return <main className="bg-background text-foreground"><section className="mx-auto grid max-w-7xl gap-10 px-6 py-12 lg:grid-cols-[1.1fr_0.9fr] lg:items-start"><div className="relative aspect-square overflow-hidden bg-muted"><Image src={product.image} alt={`${product.name} white marble`} fill className="object-cover" priority sizes="(max-width: 1024px) 100vw, 60vw" /></div><div className="pt-4 lg:pt-12"><p className="text-xs uppercase tracking-[0.22em] text-muted-foreground">Wow White Collection</p><h1 className="mt-5 font-serif text-5xl leading-tight text-balance">{product.name}</h1><p className="mt-6 max-w-lg text-base leading-7 text-muted-foreground">{product.description}</p><div className="mt-8 flex flex-wrap gap-3"><a href="tel:+919950085300" className="border border-foreground px-5 py-3 text-xs uppercase tracking-[0.16em]">Request a sample</a><a href="tel:+919829082911" className="border border-border px-5 py-3 text-xs uppercase tracking-[0.16em]">Speak to a specialist</a></div></div></section><section className="mx-auto max-w-7xl px-6 pb-16"><div className="relative aspect-[3/2] overflow-hidden"><Image src={product.landscapeImage} alt={`${product.name} marble texture`} fill className="object-cover" sizes="100vw" /></div><div className="mt-8 relative aspect-[16/9] overflow-hidden"><Image src={product.originalImage} alt={`Full lot of ${product.name} marble`} fill className="object-cover" sizes="100vw" /></div></section></main>
}
